<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/purple_700" /> <!-- Define the enabled button color (purple) -->
    <corners android:radius="8dp" />
</shape>